import React from 'react'
import { Container, Title } from './styles'
function ListLabel({ title }) {
  return (
    <Container>
      <Title>{title}</Title>
    </Container>
  )
}

export default ListLabel
